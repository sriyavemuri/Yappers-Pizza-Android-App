<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:gravity="center"
    android:padding="16dp"
    android:background="#f4f4f4">

    <!-- Title -->
    <TextView
        android:id="@+id/mainTitle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="@string/app_name"
        android:textSize="24sp"
        android:textStyle="bold"
        android:gravity="center"
        android:layout_marginBottom="24dp" />

    <!-- Navigate to Order Menu -->
    <Button
        android:id="@+id/orderMenuButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/go_to_order_menu"
        android:layout_marginBottom="16dp"
        android:padding="12dp" />

    <!-- Navigate to Order Management -->
    <Button
        android:id="@+id/orderManagementButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/manage_orders"
        android:layout_marginBottom="16dp"
        android:padding="12dp" />

    <!-- Navigate to Past Orders -->
    <Button
        android:id="@+id/pastOrdersButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/view_past_orders"
        android:padding="12dp" />
</LinearLayout>
